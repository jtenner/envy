
describe("my testing framework ", () => {
  test("the thing", () => {
    assert(42, "we are successful");
  });
  test("the other thing", () => {
    assert(0, "this is supposed to fail");
  });
});
